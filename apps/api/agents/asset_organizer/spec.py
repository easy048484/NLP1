"""asset_organizer 의 라우팅 선언. 구현이 채워지면 is_stub=False 로 바꾸세요."""

from orchestrator.registry import AgentSpec
from schemas import AgentAxis, AgentName

from .agent import run

SPEC = AgentSpec(
    name=AgentName.ASSET_ORGANIZER,
    axes=[AgentAxis.PRE_NEED, AgentAxis.POST_DEATH],
    # LLM 라우팅(name/description/examples)용 설명. 담당하지 않는 범위(경계)까지
    # 적어야 상속포기/유언/상속분/상속세 상담과 혼동되지 않는다(keywords의
    # "빚"/"채무"/"정리" 오탐 사례는 아래 keywords 주석 참고). keywords는 LLM
    # 라우터 전환이 끝날 때까지 하위호환용으로 유지한다.
    description=(
        "생전 본인 또는 사후 고인의 자산·부채를 목록화하는 에이전트. "
        "예금·주식·펀드·부동산·자동차·퇴직연금·보험·대출 등의 존재·부재, "
        "확인된 금액, 금액 미확인 상태와 안심상속 조회 결과를 수집·정리하고 "
        "검토·수정·확정까지 담당한다. 상속포기·한정승인·상속 절차 판단, "
        "유언 효력, 상속분 계산, 상속세 계산은 담당하지 않는다."
    ),
    # _classify_prompt(orchestrator/planner.py)는 example_utterances[:3]만
    # few-shot으로 쓴다. 앞 3개는 (1) 생전 재산·부채 목록화, (2) 사후 고인
    # 재산·부채 목록화(빚 언급이 상속포기 상담으로 오인되지 않게 하는 경계
    # 예시), (3) 안심상속 조회 결과 정리. 뒤 2개는 "없어요"(부재)와
    # "아직 몰라요"(금액 미확인) 표현도 담당 범위임을 보여주는 보조 예시다.
    example_utterances=[
        "내 재산이랑 빚이 뭐가 있는지 한 번 정리해보고 싶어요.",
        "어머니가 돌아가셔서 재산이랑 빚을 정리해두려고 해요.",
        "안심상속 조회 결과 예금 8천만원, 아파트 5억, 증권은 계좌만 확인됐고 대출 2천만원이에요.",
        "예금 6500만원, 펀드 1200만원 있고 주식이랑 대출은 없어요.",
        "증권 계좌가 있다는 건 확인됐는데 잔액은 아직 몰라요.",
    ],
    keywords=[
        "자산목록",
        "재산목록",
        "자산현황",
        "재산현황",
        "자산",
        "재산",
        # "정리"는 제거했다 — 일반 동사라 "상속 절차를 정리해 주세요" 같은
        # 절차/상속포기 상담에도 후보로 끼어들었다(routing false positive
        # 실측 재현). "자산"/"재산" 등 구체 키워드가 있어 정상 진입 문장은
        # 그대로 후보가 된다.
        # 아래 기관·유형명 키워드: 안심상속 조회결과·생전 자산 언급은 "자산/재산"
        # 없이 기관·유형명만 오는 경우가 많다(예: "은행은 잔액이 나왔고 증권은
        # 계좌만 확인됐어요"). 요약어만 두면 후보가 0개가 되어 last_agent에
        # 계속 붙잡혔다(실측 재현).
        "은행",
        "예금",
        "적금",
        "증권",
        "펀드",
        "부동산",
        "자동차",
        "퇴직연금",
        "보험",
        "대출",
        "잔액",
        "계좌",
        # 단독 "채무"/"빚"은 제거했다 — "정리"와 같은 문제(실측 재현): "빚이
        # 많을 것 같은데 상속포기해야 하나요?" 같은 상속포기·한정승인 상담에도
        # 후보로 끼어들었고 LLM도 걸러내지 못해 asset_organizer가 실행까지 됐다.
        # 확인·파악 의도가 문구에 드러나는 표현("빚부터 파악", "빚이 얼마")만
        # 남긴다.
        "빚 정리",
        "빚 확인",
        "빚 내역",
        "빚이 얼마",
        "빚 얼마나",
        "빚부터 파악",
        "채무 정리",
        "채무 확인",
        "채무 내역",
        "채무가 얼마",
        "채무 얼마나",
        "채무부터 파악",
        # 사후 모드 명시적 진입 — 기관·금액 없이 "안심상속 조회 결과를 정리하고
        # 싶어요"라고만 해도 후보가 돼야 한다.
        "안심상속",
    ],
    requires=[],
    produces=["asset_inventory"],
    entrypoint=run,
    is_stub=False,
)
