"""asset_organizer 의 라우팅 선언. 구현이 채워지면 is_stub=False 로 바꾸세요."""

from orchestrator.registry import AgentSpec
from schemas import AgentAxis, AgentName

from .agent import run

SPEC = AgentSpec(
    name=AgentName.ASSET_ORGANIZER,
    # AgentAxis에 pre_need/post_death 겸용을 표현할 값이 없고(tax_calculator도
    # 같은 이유로 양쪽 기능을 PRE_NEED 기본축 하나로 선언해둔 전례가 있다),
    # 이 필드는 "키워드 후보 0개일 때만" 개입하는 라우팅 폴백 힌트일 뿐이라
    # (orchestrator/planner.py._axis_default_agent 참고) 실제 사후 모드
    # 진입은 이 값과 무관하게 keywords/mode로 이미 정상 동작한다 — shared
    # schema/orchestrator 확장은 이번 작업 범위 밖.
    axis=AgentAxis.PRE_NEED,
    # 팀 전체가 키워드 의존 라우팅에서 LLM 기반(name/description/examples)
    # 라우팅으로 전환 중이라 이 설명을 상세화했다 — "무엇을 하는지"뿐 아니라
    # "무엇을 안 하는지"(경계)까지 명시해야 LLM이 상속포기/유언/상속분/
    # 상속세 상담과 헷갈리지 않는다(기존 keywords의 "빚"/"채무"/"정리" 오탐
    # 사례가 실측으로 이미 확인됐다 — 아래 keywords 주석 참고). keywords
    # 자체는 중앙 LLM 라우터 전환이 끝나기 전까지 하위호환용으로 손대지
    # 않는다.
    description=(
        "생전 본인 또는 사후 고인의 자산·부채를 목록화하는 에이전트. "
        "예금·주식·펀드·부동산·자동차·퇴직연금·보험·대출 등의 존재·부재, "
        "확인된 금액, 금액 미확인 상태와 안심상속 조회 결과를 수집·정리하고 "
        "검토·수정·확정까지 담당한다. 상속포기·한정승인·상속 절차 판단, "
        "유언 효력, 상속분 계산, 상속세 계산은 담당하지 않는다."
    ),
    # _classify_prompt(orchestrator/planner.py)는 example_utterances[:3]만
    # few-shot으로 쓴다 — 앞 3개가 생전/사후/안심상속 세 대표 시나리오를
    # 각각 담당한다: (1) 생전 본인 재산·부채 목록화 의도, (2) 사후 고인
    # 재산·부채 목록화 의도(빚 언급 포함 — "빚"만으로 상속포기 상담과
    # 혼동되면 안 된다는 경계를 예시로도 보강), (3) 안심상속 조회 결과를
    # 구조화된 금액과 함께 정리하는 문장. 뒤 2개는 보조 예시 — "없어요"
    # 부재 표현과 "아직 몰라요" 금액 미확인 표현까지 담당 범위임을
    # 보여준다.
    example_utterances=[
        "내 재산이랑 빚이 뭐가 있는지 한 번 정리해보고 싶어요.",
        "어머니가 돌아가셔서 재산이랑 빚을 정리해두려고 해요.",
        "안심상속 조회 결과 예금 8천만원, 아파트 5억, 증권은 계좌만 확인됐고 대출 2천만원이에요.",
        "예금 6500만원, 펀드 1200만원 있고 주식이랑 대출은 없어요.",
        "증권 계좌가 있다는 건 확인됐는데 잔액은 아직 몰라요.",
    ],
    keywords=[
        "자산목록",
        "재산목록",
        "자산현황",
        "재산현황",
        "자산",
        "재산",
        # "정리"는 제거했다 — 너무 일반적인 동사라 "상속 절차를 정리해
        # 주세요"처럼 재산 목록화와 무관한 절차/상속포기 상담에도 후보로
        # 잘못 끼어들었다(실측 재현, routing false positive). "자산"/"재산"
        # 등 구체적인 키워드는 이미 있어서 빠져도 정상 진입 문장은 그대로
        # 후보가 된다.
        # 안심상속 원스톱 조회결과·생전 자산 언급은 "자산/재산" 같은 요약어 없이
        # 기관·유형명만으로 오는 경우가 실제로 훨씬 많다(예: "은행은 잔액이
        # 나왔고 증권은 계좌만 확인됐어요", "예금 3200만원, 부동산 3억5천만원,
        # 대출 1억2천만원 있어요") — 위 요약어만으로는 후보가 0개가 되어
        # last_agent(직전 에이전트)에 계속 붙잡히는 문제가 있었다(실측 재현).
        "은행",
        "예금",
        "적금",
        "증권",
        "펀드",
        "부동산",
        "자동차",
        "퇴직연금",
        "보험",
        "대출",
        "잔액",
        "계좌",
        # "채무"/"빚"은 제거했다 — "정리"와 같은 클래스의 문제(실측 재현):
        # "빚이 많을 것 같은데 상속포기해야 하나요?", "채무가 많아서
        # 한정승인을 해야 하나요?"처럼 재산 목록화가 아니라 상속포기·
        # 한정승인 여부를 묻는 절차 상담에도 후보로 잘못 끼어들었다
        # (production에서 LLM도 걸러내지 못함 — asset_organizer가 실제
        # 실행까지 됨). 대신 "재산·부채 목록화" 의도가 명확한 구체 표현만
        # 남긴다 — 막연히 "빚이 많다"가 아니라 "빚부터 파악"/"빚이 얼마"처럼
        # 확인·파악 의도가 문구 자체에 있는 경우만 후보가 되게 한다.
        "빚 정리",
        "빚 확인",
        "빚 내역",
        "빚이 얼마",
        "빚 얼마나",
        "빚부터 파악",
        "채무 정리",
        "채무 확인",
        "채무 내역",
        "채무가 얼마",
        "채무 얼마나",
        "채무부터 파악",
        # 사후 모드 명시적 진입 — "안심상속"이라는 단어만으로도 후보가 돼야
        # 한다(예: "안심상속 조회 결과를 정리하고 싶어요"처럼 구체 기관·금액
        # 없이 조회결과 자체를 언급하는 경우).
        "안심상속",
    ],
    requires=[],
    produces=["asset_inventory"],
    entrypoint=run,
    is_stub=False,
)
