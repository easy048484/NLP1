# 요건 판정기(규칙 기반) 알려진 한계

작성 기준: `date_parser.py` / `requirement_checker.py` 구현 및 샘플 A/B/C 테스트 과정에서
발견된 정규식·규칙 기반 v1 엔진의 한계 목록. 새로 발견되는 항목은 여기 계속 추가할 것.

분류 기준:
- **LLM 추출 단계에서 해결 예정**: 빌드 순서 4단계(`CLAUDE.md`)에서 LLM이 텍스트 의미를 이해해
  값을 추출하게 되면 자연히 해결되는, "문맥 이해가 필요한" 유형의 한계.
- **구조적 한계**: LLM 추출로 바꿔도 원천적으로 해결되지 않거나, 텍스트만으로는 판별 자체가
  불가능한 유형의 한계 (예: 물리적 특성, 설계상 의도된 제약).

---

## 1. 성명 (`extract_name`)

### 1-1. 문장 속에 묻힌 성명 — ✅ LLM 폴백으로 해결됨
예: `"서울 강남구 거주 김영수"`, `"장남 김철수에게 물려주며, 이는 김영수의 뜻이다"`

라벨(`유언자`/`성명`/`이름`), 도장 표시(`(인)`/`(印)`), 줄 끝 단독 표기 중 어디에도 해당하지
않고 문장 중간에 이름만 섞여 있으면 정규식(`extract_name`)은 여전히 `absent`를 반환한다.

- **분류: LLM 추출 단계에서 해결 예정 → 해결됨** (`requirement_checker.extract_name_with_fallback`)
- 정규식이 `absent`를 반환했을 때만 `masking.mask_text()`를 거쳐 `llm_client.extract_testator_name()`
  을 호출해 유언자 본인 성명만 뽑는다. `.env`에 `ANTHROPIC_API_KEY`가 없거나 호출이 실패하면 조용히
  `absent`로 되돌아간다(에이전트가 죽지 않음). 어느 경로로 값이 왔는지는
  `extracted["extraction_method"]`(`"regex"|"llm"|"none"`)로 노출된다.
- 참고: 샘플 C(`서울 강남구 거주 김영수`)와 `test_decedent_name_llm_fallback.py`로 검증됨.

### 1-2. 알리아스(아호) 라벨도 콜론 필수
`_NAME_ALIAS_RE`는 `"아호: OO"` / `"호: OO"`처럼 콜론이 있어야만 인식한다. 이름 라벨은
`_NAME_LABEL_WITH_COLON_RE`(콜론 필수) / `_NAME_LABEL_LINE_START_RE`(콜론 없이도, 단 라벨이
줄 맨 앞에서 시작하고 그 줄이 "라벨 + 이름"으로 끝나야 함)로 나뉘어, 콜론 없이도 일부 인식되는
경로가 있다. 아호 라벨에는 이런 콜론-없는 경로가 없어 여전히 비일관이 남아 있다.

- **분류: LLM 추출 단계에서 해결 예정** (규칙 일관화만으로도 완화 가능하지만, "이 표기가 본명인지
  아호인지" 자체가 의미 판단이라 근본 해결은 LLM 쪽)

### 1-2-1. (수정됨) "이름"이 흔한 단어로 쓰인 문장에서의 오추출
`"특별한 이름 없음"`, `"이름 없는 사람에게 준다"`처럼 "이름"이 라벨이 아니라 평범한 단어로
쓰인 문장에서 뒤에 오는 2~4자 한글 단어(`"없음"`, `"없는"`)를 이름으로 잘못 인식하던 문제가
있었다. 콜론 없는 라벨 매칭을 "줄이 라벨+이름만으로 끝나야 함"으로 좁혀 수정했다
(`_NAME_LABEL_LINE_START_RE`). `test_name_label_word_in_ordinary_sentence_is_not_misdetected`,
`test_name_label_word_followed_by_more_prose_is_not_misdetected`로 회귀 방지.

### 1-3. 줄 끝 단독 이름 판별의 형식 의존성
`_NAME_STANDALONE_LINE_RE`는 해당 줄이 정확히 2~4자 한글로만 이루어져야 매칭된다.
`"김 영 수"`(띄어쓰기 서명), `"홍길동 올림"`, `"홍길동(자필)"`처럼 부가 문구가 붙으면 놓친다.
블록리스트(`유언장`/`유언`/`증인`/`서명`/`이상`/`끝`)도 하드코딩된 소수 단어라, 다른 문서
구조어(`"각서"`, `"확인서"`, `"별지"` 등)가 등장하면 이름으로 오인될 수 있다.

- **분류: 구조적 한계** (형식이 조금만 벗어나도 깨지는 것은 정규식 접근 자체의 근본적 제약이며,
  블록리스트를 아무리 늘려도 새로운 반례가 계속 나올 수 있음 — 다만 LLM 추출로 상당 부분 완화는 됨)

---

## 2. 주소 (`extract_address`)

### 2-1. 유언자 주소가 재산 관련 문구와 같은 줄에 있는 경우 — (재정정) 대표 예시는 LLM 폴백으로도 해결되지 않는다
`_ADDRESS_PROPERTY_CONTEXT_RE`(소유/부동산/명의로/아파트를/상속한다/증여한다 등)가 걸린 줄은
주소 후보에서 통째로 제외한다. 그런데 만약 유언자 본인 주소가 우연히 그 줄과 같은 문장에
섞여 있으면(예: `"서울 강남구에 사는 나는 내 아파트를 장남에게 상속한다"`) 진짜 주소까지
같이 제외되어 `absent`로 오판될 수 있다.

- **구조 자체는 설계대로 동작한다**
  (`requirement_checker.extract_address_with_fallback`): 정규식이 `absent`를
  반환했을 때만(=이 줄 제외로 통째로 사라졌을 때만) `masking.mask_text()`를
  거쳐 `llm_client.extract_will_address()`를 호출해 유언자 본인 주소
  문자열만 뽑는다. LLM이 찾은 문자열은 그대로 신뢰하지 않고 다시
  `_ADDRESS_UNIT_RE`/`_ADDRESS_DISTRICT_RE`에 통과시켜 `full_address`/
  `city_district_only`를 가른다 — LLM은 값만 추출하고 등급은 규칙 엔진이
  그대로 매긴다. 이 파이프라인 자체는 mock 테스트로 검증돼 있고(LLM이
  실제로 문자열을 돌려주면 정상 동작함), `#36`(도로명주소 인식 수정)도
  이 흐름에는 영향을 주지 않는다 — LLM이 애초에 문자열을 안 돌려주면
  정규식 재분류 단계까지 가지도 않기 때문이다.
- **그런데 이 문서의 대표 예시 문장 자체는, 실측으로는 한 번도 해결된 적이
  없다.** 이전에 "해결됨"/"서술이 맞았다"로 두 차례 기록했지만 둘 다
  틀렸다:
  - 2026-08-25: 코드펜스 파싱 버그로 LLM 폴백 전체가 100% 실패하던 시점이라
    애초에 검증이 불가능했는데, "정확히 추출됨을 확인했다"고 잘못 기록했다.
  - 2026-08-26, 2026-08-28(#36 반영 후): `llm_client.extract_will_address()`를
    이 문장으로 직접 호출해 6회(각 세션 3회씩) 반복 측정한 결과 **매번
    `{"address_text": null}`** — 코드펜스는 정상적으로 벗겨졌고(파싱 문제
    아님), 도로명주소 정규식 수정과도 무관하다(LLM이 null을 돌려주면
    정규식 단계 자체에 도달하지 않는다).
  - 회귀 고정: `tests/test_decedent_date_address_llm_fallback.py::test_known_limitations_2_1_example_still_returns_absent_on_real_api`
    (`@pytest.mark.live`, `pytest -q --live`로만 실행).
- **원인 추정**: `llm_client._WILL_ADDRESS_SYSTEM_PROMPT`가 "재산을 받는
  사람의 주소나 부동산 소재지를 유언자 본인 주소와 혼동하지 마라"고
  지시하는데, 이 예시 문장은 유언자 본인 주소("강남구")와 재산(아파트)이
  한 문장에 섞여 있어 모델이 어느 쪽인지 확신하지 못하고 보수적으로
  `null`을 반환하는 것으로 보인다.
- **프롬프트를 느슨하게 고치지 않는다.** "혼동하지 마라"는 지시를 완화하면
  이 예시는 풀리겠지만, 반대 방향의 위험 — 재산 소재지를 유언자 주소로
  잘못 뽑는 오탐 — 이 커진다. 주소 요건의 결과는 RED(무효 판단 판례 인용)
  까지 이어지는 값이라, "값을 못 찾으면 확신 없이 null" 쪽이 "값을 억지로
  만들어 잘못된 판정으로 이어지는" 쪽보다 안전하다. 모델이 애매한 입력에서
  확신 없이 보수적으로 `null`을 반환하는 것은 버그가 아니라 의도된 안전
  동작으로 받아들인다.
- **분류: 구조적 한계로 재분류** — "유언자 본인 주소와 재산 소재지가 한
  문장에 섞여 있을 때 어느 쪽인지 확신 있게 골라내는 것"은 LLM 폴백에
  기대하기 어렵다. 이 경우 결과는 여전히 `absent`로 남고, 사용자가 직접
  입력하거나(재추출 요청) 봉투 확인 질문(`address_envelope_check`) 경로로
  자연스럽게 넘어간다 — 조용히 실패하지 않고 사용자 확인으로 이어진다는
  점에서 안전망은 유지된다.

### 2-2. 재산 문맥 키워드가 조사(助詞) 변형에 취약
`_ADDRESS_PROPERTY_CONTEXT_RE`는 `"아파트를"`, `"건물을"`처럼 정확한 문자열만 잡는다.
`"아파트는"`, `"건물이"`, `"내가 갖고 있는 아파트"`, `"물려주고자 한다"`처럼 조사·어미가
달라지면 재산 문맥으로 인식하지 못해 부동산 주소가 다시 유언자 주소로 오인될 수 있다
(원래 있던 버그의 변형).

- **분류: 구조적 한계** (정규식은 문자열 매칭이라 한국어 교착어 특성상 조사 변화를 전부
  커버하기 어려움 — LLM은 이 문제를 상당히 줄여주지만, 규칙만으로 완전히 막기는 어려움)

### 2-3. 라벨 없는 배경 서술이 주소로 오인될 가능성 — (정정) LLM 폴백으로도 해결 안 됨
`"주소"`/`"거주"` 같은 라벨이 없어도 시/구 패턴이 감지되면 `city_district_only`로 판정한다.
`"나는 서울 강남구에서 나고 자랐다"`처럼 현재 주소가 아니라 배경 설명인 문장도 같은 방식으로
잡힐 수 있다.

- **분류: 이전에 "LLM 추출 단계에서 해결 예정"으로 표시돼 있었으나, 실제로는 대상이 아님을
  확인해 재분류 (2026-08-21)** — `extract_address_with_fallback`은 정규식이 `absent`일
  때만 LLM을 호출한다. 이 사례는 정규식이 `city_district_only`로 **뭔가를 이미 찾은**
  경우(오탐이긴 하지만 "찾음"으로 취급됨)라서 애초에 LLM이 호출되지 않는다 — 2-1(통째로
  `absent` 처리되는 케이스)과 겉보기엔 비슷해 보이지만 정규식 반환값이 다르다.
- **분류: 구조적 한계** ("이 시/구 언급이 현재 주소 표시인지 배경 서술인지"는 절 추출이
  아니라 문맥 판단이라, absent-트리거 폴백 구조로는 해결되지 않는다. 해결하려면 "정규식이
  뭔가 찾았어도 LLM이 재검토" 하는 별도 경로가 필요한데, 이는 이번 작업 범위(성명/녹음
  패턴 복제) 밖이다.)

### 2-4. 봉투 기재 여부는 본문 텍스트로 판별 불가
자필증서 유언은 주소를 유언장 본문이 아니라 봉투에 적어도 유효로 본 판례(97다38510)가
있다. 그런데 우리가 분석 대상으로 받는 텍스트는 유언장 본문뿐이라, 봉투에 뭐라고
적혀 있는지는 텍스트만으로는 원천적으로 알 수 없다.

- **분류: 구조적 한계 (단, 사용자 확인 질문으로 해결됨)** — `rules/requirements.json`의
  `address.followup`으로 전환해, 주소가 🔴로 판정된 경우에만 "주소가 봉투에 적혀
  있나요?"를 사용자에게 직접 확인한다. LLM 추출로도 봉투 내용까지는 알 수 없으므로
  4단계(LLM 추출 연결)로도 해결되지 않는 유형 — 텍스트 기반 파이프라인의 원천적 제약이라
  사용자 확인이 유일한 해법.

### 2-5. 지번(번지) 정확성 검증은 서비스 범위 밖
"지번이 실제와 근소하게 다른 경우"를 유효로 본 판례가 있다는 아이디어가 있었지만
(a) 이를 뒷받침할 판례를 아직 확보하지 못했고, (b) 설령 판례가 있어도 유언장에 적힌
지번이 실제 등기부상 지번과 "근소하게 다른지"를 판단하려면 외부 주소 데이터베이스와의
대조가 필요한데, 이는 규칙 기반이든 LLM 기반이든 이 서비스의 텍스트 분석 범위를 벗어난다.
그래서 `envelope_or_minor_discrepancy` 조건에서 이 부분은 아예 제외했다.

- **분류: 구조적 한계** (텍스트 분석만으로는 절대 해결 안 됨 — 실제 주소 대조 기능을
  별도로 붙이지 않는 한 LLM 추출 단계로 넘어가도 그대로 남는 한계)

---

## 3. 연월일 (`date_parser.py`)

### 3-1. 말로 특정하는 표현의 키워드 목록이 고정 — (정정) LLM 폴백으로도 완전히 해결되지 않음
`_VERBAL_DATE_RE`는 `칠순/팔순/구순/백수/환갑/회갑/고희/미수/백일/돌` + `(잔치/기념일)` + `에`
조합만 인식한다. `"생신"`, `"생일"`, `"결혼기념일"`, `"제사일"`, `"설날"`, `"추석"` 등 다른
기념일 표현은 아예 인식되지 않아 — 다른 날짜 표기도 없다면 `verbal_specified`가 아니라
`absent`로 떨어진다.

- **분류: LLM 추출 단계에서 해결 예정 → 부분적으로만 연결됨, 이 사례 자체는 미해결
  (2026-08-21)** — `requirement_checker.extract_date_with_fallback` 이 붙었지만,
  팀 결정에 따라 LLM은 날짜를 **원문 그대로**만 추출하고(재구성·정규화 금지),
  그 문자열을 `date_parser.parse_dates`에 **동일한** `_VERBAL_DATE_RE`로 다시
  통과시킨다. 즉 LLM이 "생신날"을 그대로 찾아 돌려줘도, 재파싱 시 여전히
  고정 키워드 목록에 없어 `absent`로 되돌아간다 — 실측 확인됨.
- **이 폴백이 실제로 도움이 되는 경우**: 이미 지원되는 형식(아라비아 숫자,
  한글 숫자, 지원 키워드)이 텍스트 전체에서 정말 아무것도 안 잡히는 경우는
  거의 없다(정규식이 전체 텍스트를 훑기 때문). 그래서 date 필드의 LLM 폴백은
  address 만큼 실질적인 개선 폭이 크지 않다 — address 는 줄 단위 제외
  규칙(2-1) 때문에 "존재하는데 통째로 가려지는" 케이스가 있지만, 날짜 파서는
  그런 줄 단위 배제 로직이 없어 같은 종류의 "가려짐" 버그가 구조적으로
  없다. 방어적 안전망으로서의 가치는 있지만, 이 항목이 실제로 해결됐다고
  보긴 어렵다.
- 키워드 목록 자체를 확장하려면 정규식에 직접 추가하는 편이 더 확실하다
  (구조적 한계 3-2/3-3과 동일하게 "규칙 추가로 해결 가능한 항목").

### 3-2. 한글 숫자 변환은 "만"(10,000) 단위 미지원
`_sino_korean_to_int`는 십/백/천 단위까지만 처리한다. 서기 연도가 사실상 항상 네 자리라
실무상 문제될 일은 거의 없지만, 만 단위가 포함된 표기(예: `"일만이십육년"`)가 들어오면
잘못 변환되거나 매칭에 실패한다.

- **분류: 구조적 한계** (범위를 의도적으로 좁힌 설계 — 발생 가능성이 낮아 우선순위 낮음)

### 3-3. 순수 숫자 연속 표기(예: `"20260503"`) 미지원
구분자(년/월/일, 점, 하이픈) 없이 8자리 숫자만 붙어 있는 형식은 인식하지 못한다.

- **분류: 구조적 한계** (별도 포맷 하나를 더 추가하는 문제이므로 규칙으로도 해결 가능하지만,
  현재 v1 범위에는 없음 — 필요 시 규칙 추가로 해결 가능한 항목이라 "LLM 없이도" 고칠 수 있음)

### 3-4. 날짜 복수 혼재 판정이 문맥을 구분하지 못함
본문에 작성일과 무관한 날짜(예: 취득일·과거 사건 날짜 등)가 함께 언급되면, 실제로는
작성일이 하나뿐이어도 `multiple_dates_mixed`로 오판될 수 있다. 어떤 날짜가 "유언장
작성일"인지 규칙만으로는 구분하지 못한다.

- **분류: LLM 자동 선별 대상 아님, 사용자 확인 질문으로 처리 예정 — 별도 이슈**
  (팀 결정 2026-08-21). 여러 날짜 중 작성일을 선별하는 것은 절 추출이 아니라
  사실 판단에 가까워, 성명·연월일(다른 항목)·주소가 쓰는 "absent 채우기"
  폴백과 신뢰 모델이 다르다. LLM이 잘못 선별해도 형식상 결과(등급)는 나오기
  때문에 실패가 조용히 묻힐 위험이 있어 이번 LLM 폴백(#날짜·주소) 범위에서
  명시적으로 제외했다 — `requirement_checker.extract_date_with_fallback`도
  `regex_result.case != "absent"`면(=`multiple_dates_mixed` 포함) LLM을
  호출하지 않는다. 대신 handwriting/seal 처럼 사용자에게 직접 "어느 날짜가
  작성일인가요?"를 묻는 확인 질문으로 처리하는 방향이 유력하나, 구체적 UX는
  별도 이슈로 남긴다.

### 3-5. 주민등록번호·전화번호가 날짜로 오탐 → 개인정보 부분 노출 — ✅ 수정 완료

**정확도 측면 (최초 발견 시 분류)**

`_YEAR_MONTH_ONLY_PATTERNS`의 `(\d{4})-(\d{1,2})` 패턴은 앞뒤 문맥과 무관하게
"4자리 숫자 + 하이픈 + 1~2자리 숫자"만 있으면 매치했다. 주민등록번호
(`"901231-1234567"`, 6자리-7자리)와 전화번호(`"010-1234-5678"`)는 둘 다 이
형태를 우연히 포함하고 있어(하이픈 앞뒤 6~7자리 숫자열 중 일부가 우연히
4+1~2자리로 잘림), 실제로는 날짜가 아닌데도 `day_missing`으로 오판됐다.

**→ 개인정보 노출 문제로 재분류 (CLAUDE.md 절대 원칙 4 위반)**

최초에는 "정확도 오탐, `date_parser` 결함"으로만 분류했으나, 노출 경로를
추적해보니 **판정 정확도만의 문제가 아니었다.** 오탐으로 만들어진
`ParsedDate`가 매칭된 원문 조각을 그대로 담아 밖으로 내보내고 있었다:

```
parse_dates()  →  ParsedDate.raw_text = "1231-12"
  →  requirement_checker.check_requirements()  "raw_text": e.raw_text
  →  RequirementResult.extracted
  →  agent._requirement_payload()
  →  AgentOutput.data["requirements"]["date"]["extracted"]["entries"][0]
```

노출되던 값은 `"1231-12"` — 주민번호 앞자리의 **생년월일(12월 31일)** 과
뒷자리 첫 숫자(**성별·세기 식별 숫자**)다. 전체 주민번호는 아니지만
생년월일+성별은 그 자체로 개인정보다. 전화번호도 같은 방식으로 `"1234-56"`이
새어나갔다.

`masking.mask_text()`로는 막히지 않는 경로였다 — 마스킹은 **LLM 호출 직전에만**
돌고, 이 유출은 LLM과 무관한 판정 결과 반환 경로에서 일어났기 때문이다.
화면 문구(`reply`)에는 나오지 않지만 API 응답 JSON에는 실렸고, 세션 저장
(네임스페이스 규약 도입 후 `DecedentState.requirements`)까지 흘러갈 수 있었다.

**수정 (두 겹)**

1. **근본 원인** — `date_parser`의 숫자 날짜 패턴 앞뒤에
   `_NO_DIGIT_BEFORE`(`(?<!\d)`) / `_NO_DIGIT_AFTER`(`(?!\d)`) 가드를 붙여
   "네 자리 연도가 더 긴 숫자열의 일부이면 날짜가 아니다"를 강제했다.
   주민번호·전화번호·구분자 없는 긴 숫자열이 전부 매칭되지 않는다.
   정상 표기(`"2026-05"`, `"2026. 5"`, `"2026년 5월"`, 전체 날짜)는 앞뒤가
   숫자가 아니므로 그대로 매칭된다.
2. **2차 방어** — 날짜 `entries` 페이로드에서 `raw_text`(매칭된 원문 조각)를
   아예 제외했다(`requirement_checker` / `recording_checker` 양쪽). 다른
   오탐이 새로 생기더라도 원문 조각 자체는 경계를 넘지 않는다. 화면 표시는
   `year`/`month`/`day`만 쓰므로 기능 손실이 없다.

**회귀 방지**: `test_decedent_date_parser.py`의 PII 파라미터 테스트 +
`test_decedent_pii_not_in_result.py`(응답·`extracted` 전체를 문자열로 훑어
조각 노출 여부 확인 — `test_*_excludes_will_text` 계열과 같은 성격).

---

## 4. 간인 (`detect_interseal`)

### 4-1. 페이지 표기 문구가 없으면 여러 장이어도 감지 불가
`(1/2)`, `"N페이지"`, `"총 N장"` 같은 명시적 표기가 텍스트에 없으면 실제로 여러 장짜리
문서여도 `single_page`로 판정된다.

- **분류: 구조적 한계** (원본 스캔/이미지 정보 없이 텍스트만으로는 물리적 장수를 알 수 없음 —
  LLM으로도 텍스트만 가지고는 해결 안 됨. OCR/이미지 메타데이터 결합이 필요)

### 4-2. 간인이 실제로 찍혔는지는 텍스트로 원천 판별 불가
간인은 각 장 사이 도장이 겹쳐 찍혔는지를 보는 것이라 텍스트만으로는 애초에 판별 대상이
아니다 (요건 자체도 법정 요건이 아니라서 참고 문구로만 처리 중).

- **분류: 구조적 한계**

---

## 5. 기타

### 5-1. 전문 자서 / 날인은 설계상 항상 사용자 확인 필요
텍스트만으로 손글씨 여부·도장/지장 여부를 판별하는 것은 원천적으로 불가능해서
`user_confirm` 방식으로 설계되어 있다 (한계라기보다 의도된 제약). 다만 사용자가 답변하지
않으면 `PENDING` 상태로 남는데, 이 질문을 언제·어떤 순서로 사용자에게 보여줄지는
`요건판정_문구_스펙_v1.md` 6절에 "미결"로 남아 있다.

- **분류: 구조적 한계** (텍스트 기반 파이프라인의 원천적 제약)

### 5-2. 오탐 방지 규칙(재산 문맥 제외 등)이 새 반례를 계속 낳을 가능성
2-1, 2-2에서 보듯 "이 줄을 제외한다"는 식의 블록리스트/키워드 기반 규칙은 새로운 문장
패턴이 나올 때마다 반례가 나올 수 있는 구조다. 규칙을 계속 추가하는 것보다, 일정 시점
이후에는 LLM 추출로 전환하는 것이 유지보수 측면에서 더 안정적이다.

- **분류: 구조적 한계** (정규식 기반 접근의 근본적 확장성 한계)

---

## 6. 녹음 (`recording_checker.py`, §1067)

### 6-1~6-4. 구어체 대본의 5개 요건 규칙 기반 추출 한계 — ✅ LLM 폴백으로 해결됨
유언자 성명/증인 성명/재산 처분 의사/증인의 정확함 확인은 전부 정규식이 특정 형태
(라벨+콜론, 줄 전체가 라벨+이름으로 끝남, 정해진 종결형 어미 등)에만 반응해서
"저는 홍길동입니다", "물려주고자 합니다"처럼 자연스러운 구어체 대본에서는 정규식만으로는
거의 항상 놓쳤다.

- **분류: LLM 추출 단계에서 해결 예정 → 해결됨** (`recording_checker.check_recording_requirements`)
- 정규식이 5개 항목(유언 취지/유언자 성명/연월일/증인의 정확함/증인 성명) 중 하나라도
  `absent`면, `masking.mask_text()`를 거쳐 `llm_client.extract_recording_fields()`를
  **한 번만** 호출해 5개를 함께 받는다 — 항목별로 따로 호출하지 않는다(비용/지연 절감).
  이미 정규식으로 찾은 항목은 LLM 값이 와도 그대로 유지한다(정규식 우선). 연월일은
  LLM이 돌려준 `date_text`도 `date_parser.parse_dates`를 다시 거쳐 일(日) 누락 등 기존
  등급 조건을 그대로 적용한다. `.env`에 `ANTHROPIC_API_KEY`가 없거나 호출이 실패하면 조용히
  각 항목의 정규식 결과(`absent`)로 되돌아간다. 어느 경로로 값이 왔는지는 항목마다
  `extracted["extraction_method"]`(`"regex"|"llm"|"none"`)로 노출된다.
- 참고: `test_decedent_recording_checker.py`의 LLM 폴백 테스트들과
  `test_decedent_recording.py::test_colloquial_transcript_all_five_green_via_llm_fallback`
  로 검증됨.
- 다만 각 항목의 정규식 자체(1차 시도)는 여전히 형태 의존적이라, LLM 폴백이 실패하거나
  키가 없는 환경에서는 원래 한계가 그대로 남는다 — LLM이 항상 켜져 있다는 전제하의
  "해결"이다.

---

## 7. 유언장 사진 판독 (`image_reader.py`, 1단계, 2026-08-25)

### 7-1. 자서(전문 자서) 판별 불가 — 구조적 한계, 의도적으로 미지원

"이 글씨가 고인 본인이 직접 쓴 것인가"는 필적 감정 영역이라 사진 한 장으로
판단할 근거가 원천적으로 부족하다. 필체가 평소와 다른지, 대필 여부, 위조
가능성 등은 전문가의 필적 대조가 필요한 사실 판단이지 "글자가 선명한지"와
전혀 다른 종류의 문제다.

- **분류: 구조적 한계 (LLM 추출 단계로도 해결 안 됨 — 의도적 설계, 설계 방침 F)**
- `image_reader.py`의 추출 대상 4필드(`PHOTO_FIELD_IDS`)에 자서는 아예
  없다 — 시스템 프롬프트에서 요청 자체를 안 한다. LLM에게 "이거 본인 필체
  같아 보여?"를 묻는 순간 그 대답이 아무리 신중하게 만들어져도 사실상의
  필적 감정 판정이 되므로, 질문 자체를 만들지 않았다.
- handwriting 요건은 이 기능 이전부터 `extraction_type: "user_confirm"`이라
  사진이 있든 없든 항상 사용자에게 직접 확인한다 — 새로 만든 우회로가
  아니라 기존 경로를 그대로 둔 것뿐이다.

### 7-2. 사진 판독 정확도는 원천적으로 사진 품질에 의존한다

흐릿한 사진, 기울어진 각도, 어두운 조명, 흘려 쓴 필체, 잉크 번짐 등은
모델이 글자를 잘못 읽거나 아예 못 읽게 만들 수 있다. `confidence`
필드로 3단(high/low/none) 완화 장치를 두었지만, 이건 모델이 스스로
"자신 없다"고 보고한 경우만 걸러낼 뿐이다 — **모델이 틀린 값을 "high"
확신도로 잘못 보고할 가능성 자체를 막지는 못한다.**

- **분류: 구조적 한계 (텍스트 추출의 3-1/3-4 항목과 같은 종류 — 신뢰
  모델이 완벽할 수 없는 영역)**
- 완화책: high-confidence로 채워진 값도 최종적으로는 기존
  `requirement_checker.py`의 정규식/등급 판정을 그대로 재통과한다(방침
  A) — 예를 들어 사진에서 "서울 강남구"까지만 읽혔다면(실제로는 번지가
  더 있었어도) `city_district_only`로 정확히 등급이 매겨진다. 즉 판독
  오류가 있어도 등급 판정 자체가 조용히 틀린 결과를 내지는 않는다(오류가
  "덜 읽음" 방향이면 더 엄격한 등급으로, "더 읽음"이나 "잘못 읽음" 방향은
  여전히 사용자 확인 없이 통과할 위험이 있음 — 1단계 범위에서 완전히
  막지는 못했다).
- 개선 여지(2단계 이후): high-confidence 임계값을 지금보다 보수적으로
  잡거나(예: 항상 한 번은 확인받기), 여러 필드가 서로 모순되면(예: 날짜가
  달력상 불가능) 확신도를 자동으로 낮추는 교차 검증을 추가하는 방향.

### 7-3. 사진 여러 장(다중 페이지) 미지원

유언장이 여러 장이면 페이지마다 사진을 올려야 할 수 있는데, 이번 1단계는
이미지 한 장(`AgentInput.image_base64` 단일 필드)만 받는다. 여러 장에
걸쳐 나뉜 항목(예: 1페이지에 본문, 마지막 페이지에 서명·날인)은 한 장씩
따로 판독해 병합하는 흐름이 없다.

- **분류: 1단계 범위 밖 (의도적 축소, 2단계 후보)**

### 7-4. `photo_confirm_answers`는 매 턴 전체 재제출을 전제한다

여러 필드가 동시에 낮은 확신도로 확인 대기 중일 때, 사용자가 한 번에
답하지 않고 여러 턴에 걸쳐 나눠 답하면 앞서 답한 내용이 사라질 수 있다.
`DecedentState.photo_confirm_answers`가 `dict` 필드라
`orchestrator/handoff.py`의 세션 병합이 필드 단위 전체 교체이기 때문이다
(다른 개별 확인 답변들 — `handwriting_answer`/`seal_answer` 등 — 은 서로
다른 최상위 필드라 이 문제가 없다).

- **분류: 구조적 한계 (1단계 범위에서 감수 — 프론트가 한 화면에 보여준
  pending_questions를 한 번에 제출한다는 전제로 설계됨)**
- 완화하려면 `handoff.build_agent_context`(공용 파일)에 이 필드 전용 딥
  머지 로직을 추가해야 하는데, decedent_estate 하나만을 위해 공용
  병합 규칙을 늘리는 것은 이번 범위에서 보류했다.
